[package]
name = "engine"
version.workspace = true
edition.workspace = true

[dependencies]

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[features]
default = ["arena_direct"]
arena_direct = []
buffered_key_writer = []
