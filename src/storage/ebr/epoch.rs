// EBR all centres around the epoch which is a global counter that is incremented
// the global epoch is referenced by all threads and is used to determine if old epochs are still being used or if they can be safely dropped
